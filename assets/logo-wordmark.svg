<!-- TokenLens – wordmark (solo texto) -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 820 180" role="img" aria-label="TokenLens wordmark">
  <defs>
    <linearGradient id="tl-g" x1="0" x2="1" y1="0" y2="1">
      <stop offset="0%"  stop-color="#6ea8fe"/>
      <stop offset="100%" stop-color="#7dd3fc"/>
    </linearGradient>
    <filter id="softGlow" x="-50%" y="-50%" width="200%" height="200%">
      <feDropShadow dx="0" dy="0" stdDeviation="2.2" flood-color="#6ea8fe" flood-opacity="0.28"/>
    </filter>
  </defs>

  <!-- guía opcional (no visible) -->
  <rect width="100%" height="100%" fill="none"/>

  <text x="30" y="122"
        font-family="Inter, system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Arial, sans-serif"
        font-size="118" font-weight="800" letter-spacing="0.5"
        fill="url(#tl-g)" filter="url(#softGlow)">
    TokenLens
  </text>
</svg>
